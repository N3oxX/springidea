job:
  commit:
    docker:
      - image: 'openjdk:11'
    steps:
      - checkout
      - run: mvn package
      - sonarcloud/scan_with_maven
orbs:
  sonarcloud: sonarsource/sonarcloud@1.0.1
version: 2.1
workflows:
  main:
    jobs:
      - commit:
          context: SonarCloud
      - sonarcloud/check_quality_gate:
          context: SonarCloud
          requires:
            - commit